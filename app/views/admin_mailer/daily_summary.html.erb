<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #374151; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
    .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .date-badge { display: inline-block; padding: 4px 12px; background: rgba(255,255,255,0.2); border-radius: 4px; font-size: 12px; }
    .stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; margin: 20px 0; }
    .stat { background: white; padding: 15px; border-radius: 4px; text-align: center; }
    .stat-value { font-size: 24px; font-weight: bold; color: #1f2937; }
    .stat-label { font-size: 11px; color: #6b7280; text-transform: uppercase; }
    .stat-change { font-size: 12px; margin-top: 5px; }
    .stat-up { color: #059669; }
    .stat-down { color: #dc2626; }
    .section { background: white; padding: 15px; border-radius: 4px; margin: 15px 0; }
    .section h3 { margin-top: 0; color: #374151; font-size: 14px; text-transform: uppercase; }
    .metric-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #f3f4f6; }
    .metric-label { color: #6b7280; }
    .metric-value { font-weight: 600; }
    .alert-item { padding: 10px; border-left: 3px solid #f59e0b; background: #fffbeb; margin: 10px 0; }
    .timestamp { color: #9ca3af; font-size: 12px; margin-top: 20px; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <span class="date-badge"><%= @date %></span>
    <h1 style="margin: 10px 0 0 0; font-size: 24px;">Daily Operations Summary</h1>
  </div>

  <div class="content">
    <div class="stats">
      <div class="stat">
        <div class="stat-value"><%= @summary[:active_tenants] || 0 %></div>
        <div class="stat-label">Active Tenants</div>
        <% if @summary[:tenant_change] %>
        <div class="stat-change <%= @summary[:tenant_change] >= 0 ? 'stat-up' : 'stat-down' %>">
          <%= @summary[:tenant_change] >= 0 ? '+' : '' %><%= @summary[:tenant_change] %>
        </div>
        <% end %>
      </div>
      <div class="stat">
        <div class="stat-value">$<%= number_with_delimiter((@summary[:mrr_cents] || 0) / 100) %></div>
        <div class="stat-label">MRR</div>
      </div>
      <div class="stat">
        <div class="stat-value"><%= @summary[:audit_events] || 0 %></div>
        <div class="stat-label">Audit Events</div>
      </div>
    </div>

    <% if @summary[:billing] %>
    <div class="section">
      <h3>Billing Activity</h3>
      <div class="metric-row">
        <span class="metric-label">Successful Payments</span>
        <span class="metric-value"><%= @summary[:billing][:successful_payments] || 0 %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Failed Payments</span>
        <span class="metric-value"><%= @summary[:billing][:failed_payments] || 0 %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">New Subscriptions</span>
        <span class="metric-value"><%= @summary[:billing][:new_subscriptions] || 0 %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Cancellations</span>
        <span class="metric-value"><%= @summary[:billing][:cancellations] || 0 %></span>
      </div>
    </div>
    <% end %>

    <% if @summary[:system] %>
    <div class="section">
      <h3>System Health</h3>
      <div class="metric-row">
        <span class="metric-label">API Requests</span>
        <span class="metric-value"><%= number_with_delimiter(@summary[:system][:api_requests] || 0) %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Background Jobs</span>
        <span class="metric-value"><%= number_with_delimiter(@summary[:system][:jobs_processed] || 0) %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Failed Jobs</span>
        <span class="metric-value"><%= @summary[:system][:jobs_failed] || 0 %></span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Avg Response Time</span>
        <span class="metric-value"><%= @summary[:system][:avg_response_ms] || 'N/A' %>ms</span>
      </div>
    </div>
    <% end %>

    <% if @summary[:alerts]&.any? %>
    <div class="section">
      <h3>⚠️ Alerts</h3>
      <% @summary[:alerts].each do |alert| %>
      <div class="alert-item">
        <strong><%= alert[:type] %></strong><br>
        <%= alert[:message] %>
      </div>
      <% end %>
    </div>
    <% end %>

    <% if @summary[:compliance] %>
    <div class="section">
      <h3>Compliance Status</h3>
      <div class="metric-row">
        <span class="metric-label">Audit Chain</span>
        <span class="metric-value" style="color: <%= @summary[:compliance][:chain_valid] ? '#059669' : '#dc2626' %>">
          <%= @summary[:compliance][:chain_valid] ? '✓ Valid' : '✗ Invalid' %>
        </span>
      </div>
      <div class="metric-row">
        <span class="metric-label">Last Verified</span>
        <span class="metric-value"><%= @summary[:compliance][:last_verified] || 'N/A' %></span>
      </div>
    </div>
    <% end %>

    <p class="timestamp">
      Report generated: <%= @timestamp %><br>
      Environment: <%= Rails.env %>
    </p>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>This is an automated daily operations summary.</p>
  </div>
</body>
</html>
