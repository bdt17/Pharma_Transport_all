<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #1e3a5f 0%, #2563eb 100%); color: white; padding: 25px; border-radius: 8px 8px 0 0; }
    .header-badge { display: inline-block; padding: 4px 12px; background: rgba(255,255,255,0.2); border-radius: 4px; font-size: 11px; text-transform: uppercase; margin-bottom: 10px; }
    .content { background: #f9fafb; padding: 25px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .chain-status { display: flex; align-items: center; padding: 15px; border-radius: 6px; margin: 20px 0; }
    .chain-valid { background: #f0fdf4; border: 1px solid #bbf7d0; }
    .chain-invalid { background: #fef2f2; border: 1px solid #fecaca; }
    .chain-icon { font-size: 24px; margin-right: 15px; }
    .stats-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; margin: 20px 0; }
    .stat-card { background: white; padding: 15px; border-radius: 6px; text-align: center; }
    .stat-value { font-size: 28px; font-weight: bold; color: #1e3a5f; }
    .stat-label { font-size: 12px; color: #6b7280; text-transform: uppercase; }
    .events-table { width: 100%; border-collapse: collapse; margin: 20px 0; }
    .events-table th { background: #f3f4f6; padding: 12px; text-align: left; font-size: 12px; text-transform: uppercase; color: #6b7280; }
    .events-table td { padding: 12px; border-bottom: 1px solid #e5e7eb; }
    .event-type { display: inline-block; padding: 2px 8px; background: #dbeafe; color: #1e40af; border-radius: 4px; font-size: 12px; }
    .signature-box { background: #f3f4f6; padding: 15px; border-radius: 6px; margin: 20px 0; font-family: monospace; font-size: 11px; word-break: break-all; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-badge">FDA 21 CFR Part 11 Compliant</div>
    <h1 style="margin: 0;">Weekly Audit Digest</h1>
    <p style="margin: 5px 0 0 0; opacity: 0.9;"><%= @period_start.strftime("%B %d") %> - <%= @period_end.strftime("%B %d, %Y") %></p>
  </div>

  <div class="content">
    <p>Hello <%= @tenant.company_name %>,</p>

    <p>Here is your weekly audit activity summary for compliance record-keeping.</p>

    <!-- Chain Integrity Status -->
    <div class="chain-status <%= @chain_verification[:valid] ? 'chain-valid' : 'chain-invalid' %>">
      <span class="chain-icon"><%= @chain_verification[:valid] ? '✓' : '⚠️' %></span>
      <div>
        <strong>Audit Chain: <%= @chain_verification[:valid] ? 'VERIFIED' : 'REQUIRES ATTENTION' %></strong><br>
        <span style="font-size: 14px; color: #6b7280;">
          <%= @chain_verification[:checked] || 0 %> records verified
        </span>
      </div>
    </div>

    <!-- Summary Statistics -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-value"><%= @summary[:total_events] %></div>
        <div class="stat-label">Audit Events</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= @summary[:subscription_events] %></div>
        <div class="stat-label">Billing Events</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= @summary[:user_events] %></div>
        <div class="stat-label">User Events</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= @summary[:system_events] %></div>
        <div class="stat-label">System Events</div>
      </div>
    </div>

    <!-- Recent Events -->
    <% if @audit_events.any? %>
    <h3>Recent Audit Events</h3>
    <table class="events-table">
      <thead>
        <tr>
          <th>Type</th>
          <th>Action</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% @audit_events.last(10).each do |event| %>
        <tr>
          <td><span class="event-type"><%= event.event_type %></span></td>
          <td><%= event.action.truncate(50) %></td>
          <td><%= event.created_at.strftime("%b %d, %H:%M") %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <% if @audit_events.count > 10 %>
    <p style="color: #6b7280; font-size: 14px;">... and <%= @audit_events.count - 10 %> more events</p>
    <% end %>
    <% end %>

    <!-- Digital Signature -->
    <h3>Digest Verification</h3>
    <p style="font-size: 14px; color: #6b7280;">
      This digest is cryptographically signed for FDA compliance verification.
    </p>
    <div class="signature-box">
      <strong>SHA-256 Signature:</strong><br>
      <%= @digest_signature %>
    </div>
    <p style="font-size: 12px; color: #9ca3af;">
      Verify this signature matches the audit chain to confirm data integrity.
    </p>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>Retain this digest for regulatory compliance documentation.</p>
    <p>Report ID: <%= @report_id %></p>
  </div>
</body>
</html>
