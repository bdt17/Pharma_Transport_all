<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: <%= @severity == 'critical' ? '#dc2626' : (@severity == 'error' ? '#ea580c' : '#f59e0b') %>; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
    .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .alert-badge { display: inline-block; padding: 4px 12px; background: rgba(255,255,255,0.2); border-radius: 4px; font-size: 12px; text-transform: uppercase; }
    .details { background: white; padding: 15px; border-radius: 4px; margin: 15px 0; }
    .details dt { font-weight: 600; color: #374151; margin-top: 10px; }
    .details dd { margin-left: 0; color: #6b7280; }
    .timestamp { color: #9ca3af; font-size: 12px; margin-top: 20px; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <span class="alert-badge"><%= @severity.upcase %></span>
    <h1 style="margin: 10px 0 0 0; font-size: 24px;">System Alert</h1>
  </div>

  <div class="content">
    <p><strong><%= @message %></strong></p>

    <% if @details.present? %>
    <div class="details">
      <dl>
        <% @details.each do |key, value| %>
        <dt><%= key.to_s.titleize %></dt>
        <dd><%= value.is_a?(Hash) ? value.to_json : value %></dd>
        <% end %>
      </dl>
    </div>
    <% end %>

    <% if @report.present? && @failed_checks.present? %>
    <div class="details">
      <h3>Failed Health Checks</h3>
      <dl>
        <% @failed_checks.each do |check, result| %>
        <dt><%= check.to_s.titleize %></dt>
        <dd><%= result[:error] || result[:status] %></dd>
        <% end %>
      </dl>
    </div>
    <% end %>

    <p class="timestamp">
      Recorded at: <%= @timestamp %><br>
      Environment: <%= Rails.env %>
    </p>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>This is an automated system notification.</p>
  </div>
</body>
</html>
