<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #dc2626; color: white; padding: 25px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #f9fafb; padding: 25px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .warning-icon { font-size: 48px; margin-bottom: 10px; }
    .alert-box { background: #fef2f2; border: 1px solid #fecaca; padding: 15px; border-radius: 6px; margin: 20px 0; }
    .attempt-info { background: white; padding: 15px; border-radius: 6px; margin: 15px 0; }
    .cta-button { display: inline-block; padding: 14px 28px; background: #1e40af; color: white; text-decoration: none; border-radius: 6px; font-weight: 600; margin: 20px 0; }
    .secondary-button { display: inline-block; padding: 12px 24px; background: white; color: #1e40af; text-decoration: none; border-radius: 6px; border: 1px solid #1e40af; margin: 10px 5px; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <div class="warning-icon">⚠️</div>
    <h1 style="margin: 0;">Payment Failed</h1>
  </div>

  <div class="content">
    <p>Hi <%= @tenant.company_name %>,</p>

    <div class="alert-box">
      <strong>We were unable to process your payment of $<%= "%.2f" % (@amount_cents / 100.0) %> USD.</strong>
    </div>

    <p>Don't worry - your service is still active, and we'll automatically retry the payment. However, please update your payment method to avoid any service interruption.</p>

    <div class="attempt-info">
      <strong>Payment Attempt <%= @attempt_count %> of 3</strong><br>
      <% if @next_attempt_date %>
      Next automatic retry: <%= @next_attempt_date.strftime("%B %d, %Y") %>
      <% end %>
    </div>

    <p><strong>Common reasons for payment failure:</strong></p>
    <ul>
      <li>Insufficient funds</li>
      <li>Expired card</li>
      <li>Card declined by bank</li>
      <li>Incorrect billing information</li>
    </ul>

    <p style="text-align: center;">
      <a href="<%= @update_payment_url %>" class="cta-button">Update Payment Method →</a>
    </p>

    <p style="text-align: center; color: #6b7280;">
      <a href="<%= @billing_portal_url %>" class="secondary-button">View Billing Portal</a>
      <a href="mailto:support@pharmatransport.io" class="secondary-button">Contact Support</a>
    </p>

    <% if @attempt_count >= 3 %>
    <div class="alert-box" style="background: #fef3c7; border-color: #fcd34d;">
      <strong>⚠️ Final Notice:</strong> This is your last payment attempt. Please update your payment method immediately to avoid service suspension.
    </div>
    <% end %>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>Questions? Reply to this email or contact support@pharmatransport.io</p>
  </div>
</body>
</html>
