<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #1e40af; color: white; padding: 25px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #f9fafb; padding: 25px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .info-box { background: #f0f9ff; border: 1px solid #bae6fd; padding: 15px; border-radius: 6px; margin: 20px 0; }
    .renewal-details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; }
    .detail-row { display: flex; justify-content: space-between; padding: 12px 0; border-bottom: 1px solid #f3f4f6; }
    .detail-row:last-child { border-bottom: none; }
    .detail-label { color: #6b7280; }
    .detail-value { font-weight: 600; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0;">Subscription Renewal Reminder</h1>
  </div>

  <div class="content">
    <p>Hi <%= @tenant.company_name %>,</p>

    <p>This is a friendly reminder that your Pharma Transport subscription will automatically renew soon.</p>

    <div class="renewal-details">
      <h3 style="margin-top: 0;">Renewal Details</h3>

      <div class="detail-row">
        <span class="detail-label">Plan</span>
        <span class="detail-value"><%= @plan_name %></span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Renewal Date</span>
        <span class="detail-value"><%= @renewal_date.strftime("%B %d, %Y") %></span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Amount</span>
        <span class="detail-value">$<%= "%.2f" % (@amount_cents / 100.0) %> USD</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Payment Method</span>
        <span class="detail-value"><%= @payment_method || "Card on file" %></span>
      </div>
    </div>

    <div class="info-box">
      <strong>No action required</strong><br>
      Your subscription will automatically renew using your payment method on file. If you'd like to make any changes, visit your billing portal before the renewal date.
    </div>

    <p style="text-align: center;">
      <a href="<%= @billing_portal_url %>" style="color: #1e40af; font-weight: 600;">Manage Subscription →</a>
    </p>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>Thank you for being a valued customer.</p>
  </div>
</body>
</html>
