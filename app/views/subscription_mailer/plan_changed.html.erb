<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #1e40af 0%, #7c3aed 100%); color: white; padding: 25px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #f9fafb; padding: 25px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .plan-comparison { display: flex; justify-content: center; align-items: center; margin: 25px 0; }
    .plan-box { background: white; padding: 20px; border-radius: 8px; text-align: center; min-width: 150px; }
    .plan-box.old { opacity: 0.6; }
    .plan-box.new { border: 2px solid #1e40af; }
    .plan-arrow { font-size: 24px; margin: 0 20px; color: #1e40af; }
    .plan-name { font-size: 18px; font-weight: bold; margin-bottom: 5px; }
    .plan-price { color: #6b7280; }
    .changes { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; }
    .change-item { display: flex; align-items: center; padding: 10px 0; border-bottom: 1px solid #f3f4f6; }
    .change-item:last-child { border-bottom: none; }
    .change-icon { margin-right: 10px; }
    .change-add { color: #059669; }
    .change-remove { color: #dc2626; }
    .footer { text-align: center; padding: 20px; color: #9ca3af; font-size: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0;">Plan Updated!</h1>
    <p style="margin: 10px 0 0 0; opacity: 0.9;">Your subscription has been changed</p>
  </div>

  <div class="content">
    <p>Hi <%= @tenant.company_name %>,</p>

    <p>Your subscription plan has been successfully updated.</p>

    <div class="plan-comparison">
      <div class="plan-box old">
        <div class="plan-name"><%= @old_plan %></div>
        <div class="plan-price">$<%= "%.2f" % (@old_price_cents / 100.0) %>/mo</div>
      </div>
      <div class="plan-arrow">→</div>
      <div class="plan-box new">
        <div class="plan-name"><%= @new_plan %></div>
        <div class="plan-price">$<%= "%.2f" % (@new_price_cents / 100.0) %>/mo</div>
      </div>
    </div>

    <% if @new_features&.any? || @removed_features&.any? %>
    <div class="changes">
      <h3 style="margin-top: 0;">What's Changed</h3>

      <% @new_features&.each do |feature| %>
      <div class="change-item">
        <span class="change-icon change-add">✓</span>
        <span><strong>Added:</strong> <%= feature %></span>
      </div>
      <% end %>

      <% @removed_features&.each do |feature| %>
      <div class="change-item">
        <span class="change-icon change-remove">✗</span>
        <span><strong>Removed:</strong> <%= feature %></span>
      </div>
      <% end %>
    </div>
    <% end %>

    <p><strong>Effective:</strong> <%= @effective_date&.strftime("%B %d, %Y") || "Immediately" %></p>

    <% if @proration_amount_cents && @proration_amount_cents != 0 %>
    <p style="background: #f0fdf4; padding: 15px; border-radius: 6px;">
      <strong>Billing adjustment:</strong>
      <% if @proration_amount_cents > 0 %>
      A prorated charge of $<%= "%.2f" % (@proration_amount_cents / 100.0) %> will be added to your next invoice.
      <% else %>
      A credit of $<%= "%.2f" % (@proration_amount_cents.abs / 100.0) %> has been applied to your account.
      <% end %>
    </p>
    <% end %>
  </div>

  <div class="footer">
    <p>Pharma Transport - FDA 21 CFR Part 11 Compliant</p>
    <p>Questions? Contact support@pharmatransport.io</p>
  </div>
</body>
</html>
