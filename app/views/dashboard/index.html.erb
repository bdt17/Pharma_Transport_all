<!DOCTYPE html>
<html>
<head>
  <title>Pharma GPS Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root {
      --primary-blue: #0984C0;      /* Deep Ocean Blue */
      --accent-silver: #AAA7B0;     /* Metallic Silver */
      --mid-grey: #565759;          /* Davy's Grey */
      --bg-white: #FFFFFF;          /* Pure White */
      --light-variant: #C0BEC6;     /* Silver Sand */
      --lighter-blue: #60BDD1;      /* Sea Serpent */
    }
    
    body { 
      font-family: -apple-system, BlinkMacSystemFont, sans-serif; 
      margin: 0; padding: 20px; 
      background: var(--bg-white);
      color: var(--mid-grey);
    }
    
    .header { 
      background: var(--primary-blue); 
      color: var(--bg-white); 
      padding: 2rem; 
      border-radius: 12px; 
      margin-bottom: 2rem; 
      text-align: center; 
      box-shadow: 0 4px 12px rgba(9,132,192,0.3);
    }
    
    .stats { 
      display: grid; 
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
      gap: 1.5rem; 
      margin-bottom: 2rem; 
    }
    
    .stat-card { 
      background: var(--bg-white); 
      padding: 1.5rem; 
      border-radius: 12px; 
      box-shadow: 0 2px 8px rgba(170,167,176,0.2);
      border-left: 4px solid var(--primary-blue);
    }
    
    .vehicles { 
      background: var(--bg-white); 
      padding: 1.5rem; 
      border-radius: 12px; 
      box-shadow: 0 2px 8px rgba(170,167,176,0.2);
      border: 1px solid var(--accent-silver);
    }
    
    .vehicle { 
      display: flex; 
      justify-content: space-between; 
      padding: 1rem 0; 
      border-bottom: 1px solid var(--light-variant); 
    }
    
    .vehicle:last-child { border-bottom: none; }
    
    .status-live { 
      color: var(--primary-blue); 
      font-weight: bold; 
    }
    
    .status-idle { 
      color: var(--accent-silver); 
      font-weight: bold; 
    }
    
    .coords { 
      font-family: monospace; 
      font-size: 0.9em; 
      color: var(--mid-grey); 
    }
    
    a { color: var(--lighter-blue); text-decoration: none; }
    a:hover { text-decoration: underline; }
    
    h1, h2, h3 { color: var(--primary-blue); }
  </style>
</head>
<body>
  <div class="header">
    <h1>🚚 Pharma Transport GPS</h1>
    <p><strong>Phase 2: LIVE</strong> | Real-time tracking operational</p>
  </div>

  <div class="stats">
    <div class="stat-card">
      <h3>Vehicles Active</h3>
      <div style="font-size: 2.5rem; font-weight: bold; color: var(--primary-blue);">
        <%= Vehicle.count %>
      </div>
    </div>
    <div class="stat-card">
      <h3>Geofences</h3>
      <div style="font-size: 2.5rem; font-weight: bold; color: var(--primary-blue);">
        <%= Geofence.count %>
      </div>
    </div>
    <div class="stat-card">
      <h3>Recent Alerts</h3>
      <div style="font-size: 2.5rem; font-weight: bold; color: var(--accent-silver);">
        0
      </div>
    </div>
  </div>

  <div class="vehicles">
    <h2>📍 Live Fleet Tracking</h2>
    <% if Vehicle.any? %>
      <% Vehicle.order(created_at: :desc).each do |vehicle| %>
        <div class="vehicle">
          <div>
            <strong><%= vehicle.name %></strong>
            <div class="coords">
              Lat: <%= vehicle.latitude %> | Lng: <%= vehicle.longitude %>
            </div>
          </div>
          <div class="status-<%= vehicle.created_at > 5.minutes.ago ? 'live' : 'idle' %>">
            <%= vehicle.created_at > 5.minutes.ago ? '🟢 LIVE' : '🟡 IDLE' %>
          </div>
        </div>
      <% end %>
    <% else %>
      <p>No vehicles online. Add via Rails console: <code>Vehicle.create!(name: "Truck 001", latitude: 33.4484, longitude: -112.0740)</code></p>
    <% end %>
  </div>

  <script>
    setTimeout(() => location.reload(), 10000); // 10s polling
  </script>
</body>
</html>
